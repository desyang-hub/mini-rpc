## 实现动态代理和远程调用

### 第 5-7 天：动态代理与 RPC 调用流程
任务：
* 服务端：维护一个 ServiceMap（哈希表），Key是"服务名.方法名"，Value是函数指针/回调。收到请求后查表执行。
* 客户端：封装 RpcChannel。用户调用 stub->Method() -> 序列化 -> 发送网络请求 -> 等待响应 -> 反序列化 -> 返回。
* 难点：同步与异步。先实现同步调用（阻塞等待），有余力再上 std::future 或回调实现异步。